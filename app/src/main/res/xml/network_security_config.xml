<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Empty = no trusted CAs = no outbound connections -->
        </trust-anchors>
    </base-config>
</network-security-config>
